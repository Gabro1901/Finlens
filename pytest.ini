[tool:pytest]
markers =
    integration: Tests that require network access to external APIs (EDGAR, Yahoo, etc.)
    slow: Tests that take significant time to run (full pipeline)
asyncio_default_fixture_loop_scope = function
